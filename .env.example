# FindQC 微服务配置文件模板
# 复制此文件为 .env 并根据实际情况修改配置

# ==========================================
# 数据库配置
# ==========================================
# 默认使用 SQLite（开发/测试），无需配置
# 如需使用 MySQL，取消下面的注释并配置
# USE_MYSQL=true
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=your_password
DB_NAME=findqc_db

# ==========================================
# 爬虫服务配置（service_spider）
# ==========================================
# 分类ID范围（遍历的分类ID范围）
START_CAT_ID=3000
END_CAT_ID=10000

# 爬取模式
# MAX_PRODUCTS=10  # 测试模式：只爬取指定数量
# MAX_PRODUCTS=0   # 全量模式：不限制（或不设置）

# 并发配置
MAX_CONCURRENT_CATEGORIES=50  # 并发处理的分类数量（类似旧项目的多线程，默认8）

# ==========================================
# 日志配置
# ==========================================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# ==========================================
# FindQC API 配置（可选）
# ==========================================
# FINDQC_API_BASE_URL=https://findqc.com/api
# FINDQC_API_KEY=your_api_key

# ==========================================
# RabbitMQ 配置（可选，消息队列服务未启用时可忽略）
# ==========================================
# RABBITMQ_HOST=localhost
# RABBITMQ_PORT=5672
# RABBITMQ_USER=guest
# RABBITMQ_PASSWORD=guest
# RABBITMQ_VHOST=/

# ==========================================
# AI 服务配置（service_ai_pipe，暂未实现）
# ==========================================
# QWEN_API_BASE_URL=
# QWEN_API_KEY=
# GOOGLE_LENS_API_KEY=

# ==========================================
# 阿里云图搜配置（service_cluster，暂未实现）
# ==========================================
# ALIYUN_IMAGE_SEARCH_ENDPOINT=
# ALIYUN_IMAGE_SEARCH_ACCESS_KEY_ID=
# ALIYUN_IMAGE_SEARCH_ACCESS_KEY_SECRET=

# API 重试配置
API_RETRY_MAX_ATTEMPTS=3  # 最大重试次数（默认3次）
API_RETRY_DELAY=2.0  # 初始延迟秒数（默认2秒）
API_RETRY_BACKOFF=2.0  # 退避倍数（每次重试延迟 = 上次延迟 * backoff，如 2s -> 4s -> 8s）
